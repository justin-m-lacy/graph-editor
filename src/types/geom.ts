
export interface TPoint {
	uid: string,
	id: string,

	color?: string,
	blur?: number,
	r: number,

	x: number, y: number
}
export interface Constellation {

	uid: string;
	id: string;

	color?: string;

	stars: string[]

}